export enum ViewType {
    day,
    month,
    year    
}

export enum DateChangeType {
    day,
    nextDay,
    prevDay,
    nextMonth,
    prevMonth,
    nextYear,
    prevYear,
    month,
    year
}